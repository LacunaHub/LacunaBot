import { createCaseEntry } from './CaseLog'
import { addWarn } from './Warnings'

export const caseLog = { createCaseEntry }
export const warnings = { addWarn }

export default {
    caseLog,
    warnings
}
